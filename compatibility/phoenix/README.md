# Phoenix reference oracle

This intentionally small Phoenix application is the executable upstream oracle
for LiveViewJS compatibility work. Its Mix and JavaScript lockfiles are part of
the conformance contract; do not update them without updating
`../liveview.json` and regenerating the reviewed fixtures.

See [`../README.md`](../README.md) for setup, recording, normalization, and
verification commands.
